---
reads:
  - docs/truth/modules/bookmark.md
  - docs/truth/sources/S-01_书签PRD/digest.md
covers:
  - S-01.R-003
---

# T-003 · 标签搜索

## 目标

`bm add --tag` 与 `bm find --tag` 按 PRD §3 落地。

## In / Out

- In：添加带多标签；按标签搜索，多标签取交集
- Out：标签重命名 / 删除管理（PRD 未要求）

## Done Contract

- `add --tag 工作,学习` 后 `find --tag 工作` 能命中、`find --tag 工作,生活` 不命中；有单测证据

## 实现依据

PRD §3 原文细节：标签随 add 以 `--tag a,b` 传入（逗号分隔、可多个）；find 按标签过滤，多标签为交集语义。（实现读原文这一节，摘编只是导航。）

## Checkpoint（RIPER）

- 当前理解 ✓ · 已完成：数据模型加 tags 字段 · 下一步：find 交集逻辑 + 单测 · 风险：大小写归一待 Open Question 裁决 · 验证：单测

## 验证记录

（进行中）

## 偏差记录

（无）
